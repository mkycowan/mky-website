import { PostLayout } from "@components/layouts/PostLayout.tsx";

export const meta = {
  author: "Michael Cowan",
  title: "Year of the Hermit",
};

export default ({ children }) => (
  <PostLayout meta={meta}>{children}</PostLayout>
);

# Year of the Hermit

This is my first blog post.
