import { PostLayout } from "@components/layouts/post-layout.tsx";

export const meta = {
  author: "Michael Cowan",
  title: "Micro-dose of Micro Frontends",
  slug: "micro-dose-of-micro-frontend",
  publishDate: "March 14, 2022",
  category: "microfrontend",
};

export default ({ children }) => (
  <PostLayout meta={meta}>{children}</PostLayout>
);

# Year of the Hermit

This is my first blog post.
